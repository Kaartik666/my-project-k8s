apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: {{ .Values.namespace }}
data:
  NODE_ENV: {{ .Values.configmap.NODE_ENV | quote }}
  PORT: {{ .Values.configmap.PORT | quote }}
